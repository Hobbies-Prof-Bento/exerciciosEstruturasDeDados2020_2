/*Faça o teste de mesa do código abaixo, com n=5:*/
int pesqSeqSent(int chave, int v[], int n) {
	int i = 0; 
	vet[n] = chave; 
	while (vet[i] != chave) {
		i++; 
	} 
	if( i < n ) 
		return i; 
	return (-1); // Índice inválido 
} 

